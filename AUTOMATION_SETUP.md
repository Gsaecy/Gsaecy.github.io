# AI博客自动化系统 - 快速设置指南

## 🚀 系统概述

这是一个完全自动化的AI行业分析博客系统，能够：
1. **自动采集**行业资讯数据
2. **AI智能分析**趋势和机会
3. **自动生成**专业博客文章
4. **定时发布**到GitHub Pages

## ⚙️ 快速设置

### 1. 配置GitHub Secrets

在GitHub仓库设置中，添加以下Secret：

1. 进入仓库 Settings → Secrets and variables → Actions
2. 点击 "New repository secret"
3. 添加以下Secret：

| Secret名称 | 值 | 说明 |
|-----------|-----|------|
| `DEEPSEEK_API_KEY` | 你的DeepSeek API密钥 | 用于AI分析 |

### 2. 验证配置

1. 进入仓库的 "Actions" 标签页
2. 选择 "Ultimate Blog Workflow"
3. 点击 "Run workflow"
4. 选择操作类型为 "verify"
5. 点击 "Run workflow"

### 3. 测试完整流程

1. 再次运行 "Ultimate Blog Workflow"
2. 选择操作类型为 "publish"
3. 等待流程完成（约5-10分钟）

## 📅 自动化计划

系统已配置以下自动化任务：

| 任务 | 时间 | 说明 |
|------|------|------|
| **每日分析** | 每天10:00（北京时间） | 自动采集、分析、生成文章 |
| **手动触发** | 随时 | 在Actions页面手动运行 |
| **代码推送** | 每次push到main分支 | 自动测试和部署 |

## 🔧 手动运行

### 本地测试

```bash
# 克隆仓库
git clone https://github.com/Gsaecy/Gsaecy.github.io.git
cd Gsaecy.github.io

# 安装依赖
pip install -r requirements.txt

# 设置API密钥（可选）
export DEEPSEEK_API_KEY="你的API密钥"

# 运行自动化系统
python scripts/simple_automation.py

# 本地预览博客
hugo server -D
```

### GitHub Actions手动触发

1. 访问：`https://github.com/Gsaecy/Gsaecy.github.io/actions`
2. 选择 "Daily AI Blog Automation"
3. 点击 "Run workflow"
4. 可选择测试模式或正常模式

## 📊 系统架构

```
┌─────────────────────────────────────────┐
│           GitHub Actions                │
├─────────────────────────────────────────┤
│ 1. 触发工作流                           │
│ 2. 运行AI自动化脚本                     │
│ 3. 生成博客文章                         │
│ 4. 提交到仓库                           │
│ 5. 触发Hugo构建                         │
│ 6. 部署到GitHub Pages                   │
└─────────────────────────────────────────┘
                     │
┌─────────────────────────────────────────┐
│            AI自动化系统                  │
├─────────────────────────────────────────┤
│ • 数据采集（模拟/真实）                  │
│ • AI分析（DeepSeek API）                │
│ • 内容生成（Markdown格式）               │
│ • 结果保存（JSON日志）                   │
└─────────────────────────────────────────┘
```

## 🐛 故障排除

### 常见问题

#### 1. GitHub Actions失败
- **检查Secret配置**：确保`DEEPSEEK_API_KEY`已正确设置
- **查看日志**：点击失败的job查看详细错误信息
- **权限问题**：确保workflow有正确的write权限

#### 2. 文章未生成
- **API密钥问题**：检查DeepSeek API密钥是否有效
- **网络问题**：GitHub Actions可能需要访问外部API
- **脚本错误**：查看`logs/`目录中的错误日志

#### 3. 博客未更新
- **部署延迟**：GitHub Pages部署可能需要几分钟
- **构建失败**：检查Hugo构建日志
- **缓存问题**：浏览器可能需要清除缓存

### 日志查看

```bash
# 查看系统日志
cat logs/*.log

# 查看数据文件
ls -la data/raw/
ls -la data/analysis/

# 查看生成的文章
ls -la content/posts/
```

## 🔄 扩展系统

### 添加真实数据源

编辑 `scripts/simple_automation.py` 中的 `collect_sample_data()` 方法，替换为真实的数据采集：

```python
def collect_real_data(self):
    """从真实数据源采集数据"""
    # 示例：从RSS feed采集
    import feedparser
    
    feeds = [
        "https://rss.example.com/tech",
        "https://rss.example.com/finance"
    ]
    
    articles = []
    for feed_url in feeds:
        feed = feedparser.parse(feed_url)
        for entry in feed.entries[:5]:
            articles.append({
                "title": entry.title,
                "content": entry.summary,
                "source": feed_url,
                "category": self.categorize_article(entry.title),
                "timestamp": datetime.datetime.now().isoformat(),
                "url": entry.link
            })
    
    return articles
```

### 自定义AI分析提示

编辑 `config/config.yaml` 中的分析参数：

```yaml
analysis:
  ai_model:
    temperature: 0.7  # 控制创造性（0-1）
    max_tokens: 2000  # 最大输出长度
  
  parameters:
    trend_analysis_depth: "deep"
    include_opportunities: true
    include_risks: true
```

### 修改博客主题

1. 编辑 `hugo.toml` 中的主题设置
2. 或在 `themes/` 目录中添加新主题

## 📈 监控和维护

### 系统状态检查

1. **GitHub Actions状态**：查看最近的工作流运行
2. **博客访问状态**：访问 https://gsaecy.github.io
3. **数据存储**：检查 `data/` 和 `logs/` 目录

### 定期维护

1. **清理旧数据**：定期清理 `data/raw/` 中的旧文件
2. **更新依赖**：定期更新 `requirements.txt`
3. **备份配置**：备份 `config/config.yaml`

## 🆘 获取帮助

### 问题反馈

1. **GitHub Issues**：提交问题到仓库
2. **查看文档**：阅读项目README和代码注释
3. **检查示例**：参考现有的工作流配置

### 紧急恢复

如果系统完全失败：

1. **回滚更改**：使用git回滚到上一个工作版本
2. **手动发布**：手动创建文章并提交
3. **简化流程**：暂时使用模拟数据模式

## 🎯 成功指标

系统正常运行的表现：

- ✅ GitHub Actions每日自动运行
- ✅ 新文章出现在 `content/posts/` 目录
- ✅ 博客网站每日更新
- ✅ 无错误日志生成

---

**系统版本**: v1.0  
**最后更新**: 2026-02-02  
**支持**: GitHub Issues 或 项目文档